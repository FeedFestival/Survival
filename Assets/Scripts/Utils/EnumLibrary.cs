using UnityEngine;

namespace Assets.Scripts.Utils
{
    public enum WallState
    {
        Show,
        Hide
    }

    public enum DoorState
    {
        Still, Open, Close
    }

    public enum GlassBlinds
    {
        Closed, Opened
    }

    public enum Direction
    {
        Forward, Backwards
    }
}